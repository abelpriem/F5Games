package org.f5games.aiden_game.models;

import static org.hamcrest.MatcherAssert.assertThat;
import static org.hamcrest.Matchers.is;
import java.lang.reflect.Field;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

public class BackpackTest {

// INSERT INTO game_objects (id,name,description,special) VALUES (default,'Espada encantada','Aumenta el daño del héroe',false);
    private Backpack backpack;

    @BeforeEach
    void setUp() {
       // backpack = new Backpack(1L, GameObject.builder().description("mock description"));
    }

    @Test
    public void blablab() {


    }



}
